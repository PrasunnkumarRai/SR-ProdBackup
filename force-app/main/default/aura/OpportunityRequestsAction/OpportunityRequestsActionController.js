({
	createNewRequest : function(component, event, helper) {
		helper.createSelectedRequest( component, event, helper );
	},
    dismissQuickAction : function( component, event, helper ){
        helper.dismissQuickAction( component, event, helper );
    }
})